extends layout

block content
  h1= title
  p Welcome to #{title}
  p This is Shanmuk Sai Palnati.
  button(type="button", class="btn btn-primary" onClick="doReduce()") PressMe
  div(id="reduce") Reduce the value.
  a(href="/student") Go to Student.
  script. 
      let count = 20;
      let change = -1;
      function doReduce(){
        count += change;
        change -= 1;
        document.getElementById("reduce").innerHTML = "Count "+count;
      }